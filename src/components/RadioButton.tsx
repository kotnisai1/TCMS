import React, { useState } from "react";
import RegisterCustomer from "../pages/RegisterCustomer";

function RadioButtons() {
  /* const [selectedOption, setSelectedOption] = useState("registerCustomer");
  const handleOptionChange = (e: any) => {
    setSelectedOption(e.target.value);
  };
  return (
    <div>
      <input
        type="radio"
        id="registerCustomer"
        value="registerCustomer"
        checked={selectedOption === "registerCustomer"}
        onChange={handleOptionChange}
      />
      <label htmlFor="registerCustomer">Register Customer</label>
      <input
        type="radio"
        id="existingCustomer"
        value="existingCustomer"
        checked={selectedOption === "existingCustomer"}
        onChange={handleOptionChange}
      />
      <label htmlFor="existingCustomer">Existing Customer</label>
    </div>*/
}

export default RadioButtons;
